apiVersion: v1
kind: Pod
metadata:
  name: matt-test
  namespace: application
spec:
  containers:
#  - image: acrdevakshislmgtuksouth001abfimd.azurecr.io/testimages/nginx:latest
  - image: memcached
    name: matt-test
    resources:
      requests:
        memory: "64Mi"
        cpu: "250m"
      limits:
        memory: "128Mi"
        cpu: "500m"
